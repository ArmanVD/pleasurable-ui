<header class="veronica-header">
  <section class="upper-header">
    <img src="/assets/veronica-logo.svg" alt="veronica logo">
    <input id="switch" type="checkbox">
    <label for="switch"></label>
  </section>

  <section class="footer-becomes-header">
    <nav>
      <ul>
        <li>
          <a href="/radio-veronica">Veronica</a>
        </li>
        <li>
          <a href="/slam">Slam</a>
        </li>
        <li>
          <a href="/100nl">100%nl</a>
        </li>
        <li>
          <a href="/livechat">livechat!</a>
        </li>
        <li>
          <input id="switch" type="checkbox">
          <label for="switch"></label>
        </li>
      </ul>
    </nav>
  </section>
</header>

<section class="hoofd-nav">
  <nav>
    <ul>
      <li><a href="/radio-veronica">progammering</a></li>
      <li><a href="/veronica/dj">DJ's</a></li>
      <li><a href="/veronica/playlist">playlist</a></li>
      <li><a href="/veronica/frequenties">frequenties</a></li>
      <li><a href="/veronica/likes">likes</a></li>
    </ul>
  </nav>
</section>

<script>
  const themeToggles = document.querySelectorAll('#switch');

  themeToggles.forEach(toggle => {
    toggle.addEventListener('change', () => {
      const isDark = toggle.checked;
      themeToggles.forEach(t => {
        if (t !== toggle) t.checked = isDark;
      });

      document.documentElement.setAttribute('data-theme', isDark ? 'dark' : 'light');
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
    });
  });

  // On page load, apply saved theme or system preference
  window.addEventListener('DOMContentLoaded', () => {
    const savedTheme = localStorage.getItem('theme');
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    const isDark = savedTheme ? savedTheme === 'dark' : prefersDark;

    document.documentElement.setAttribute('data-theme', isDark ? 'dark' : 'light');
    themeToggles.forEach(t => t.checked = isDark);
  });
</script></file>